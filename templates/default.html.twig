{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="contents">
        <h1>{{ page.title }}</h1>
    </div>
    <div class="contents transition">
        {{ page.content|raw }}
    </div>
{% endblock %}